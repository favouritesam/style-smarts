server.port=8082

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/stylesmarts
spring.datasource.username=postgres
spring.datasource.password=chiChi1212$
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Flyway Configuration (if you are using it)
# spring.flyway.enabled=true

# JWT Configuration
# Secret key for JWT token signing (must be at least 256 bits for HS256)
jwt.secret=mySecretKeyForJWTTokenGenerationThatIsAtLeast256BitsLong
# Token expiration time in milliseconds (86400000 = 24 hours)
jwt.expiration=86400000


cloudinary.cloud-name=dvzxk9ypm
cloudinary.api-key=223727625878618
cloudinary.api-secret=_b5busOw4KdGaa3Zps8Z3d7yeuI



# The SMTP server host - we are using Gmail's SMTP server
spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.username=favsamlol@gmail.com
spring.mail.password=qwnr hbvk kjcn pcyr
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.auth=true
# Connection timeout in milliseconds (10 seconds)
spring.mail.properties.mail.smtp.connectiontimeout=10000
# Read timeout in milliseconds (10 seconds)
spring.mail.properties.mail.smtp.timeout=10000
# Write timeout in milliseconds (10 seconds)
spring.mail.properties.mail.smtp.writetimeout=10000
# The "From" display name shown in the recipient's inbox
app.mail.from-name=StyleSmart Team