server.port=8082

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/stylesmarts
spring.datasource.username=postgres
spring.datasource.password=chiChi1212$
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Flyway Configuration (if you are using it)
# spring.flyway.enabled=true

# JWT Configuration
# Secret key for JWT token signing (must be at least 256 bits for HS256)
jwt.secret=mySecretKeyForJWTTokenGenerationThatIsAtLeast256BitsLong
# Token expiration time in milliseconds (86400000 = 24 hours)
jwt.expiration=86400000
